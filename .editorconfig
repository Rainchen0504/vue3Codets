#配置编码风格文件，保证多端多编辑器上维护编码风格一致
#vscode不会主动读取，需要安装插件才能运行editorconfig

root = true

[*] #表示文件通用规范
charset = utf-8 #设置文件字符集为utf-8
indent_style = space #缩进风格
indent_size = 2  #缩进大小
end_of_line = lf  #控制换行类型
trim_trailing_whitespace = true  #去除首行的任意空白字符串
insert_final_newline = true  #始终在文件的末尾插入一个新行

[*.md] #表示仅 md文件适用以下规则
max_line_length = off
trim_trailing_whitespace = false
